import Loading from "@/components/ui/loading";

export default function LoadingPage() {
    return (
        <div className="flex min-h-screen w-full grow flex-col items-center justify-center">
            <Loading />
        </div>
    );
}
